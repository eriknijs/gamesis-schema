asyncapi: 2.5.0
id: uri:gamesis.mail.model
info:
    title: Gamesis Mail Service
    version: 1.0.0
    description: This service accepts a request to send a templated message
    contact:
        email: info@gamesis.nl
        name: Erik Nijs
        url: https://gamesis.nl
    license:
        name: Apache-2.0
        url: https://www.apache.org/licenses/LICENSE-2.0
tags: 
    - name: player
    - name: email
servers:
    default:
        url: ${host}
        protocol: jms
channels:
    mail:
        subscribe:
            operationId: requestMail
            summary: An event occured for which the end user must be notified by mail
            message:
                $ref: 'components.yaml#/components/messages/MailRequest'